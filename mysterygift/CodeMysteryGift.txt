####################################################
# BASE DE REGALO MISTERIOSO PARA EL SCRIPT DE REGALOS MISTERIOSOS AVANZADOS DE CLARA
# Tómalo de base para crear o añadir nuevos eventos.
# Subelo a Pastebin o a Github como un txt y coge el enlace del archivo en RAW.
# Este archivo tiene un ejemplo de códigos.
#
# Igual que en OnlineMysteryGift.txt, no hace falta comprobar tú mismo si el
# regalo ya se recibió ni avisar de "código incorrecto": si el código no
# coincide con ningún "when", `gifts` se queda vacío y pbProcessGiftsList ya
# muestra el aviso correspondiente.
####################################################
password = pbEnterText(_INTL("Inserta tu código."),0,12).upcase

if password == "CR0WN" && GAME_CODE != "VITRO"
  gifts.push(MysteryGiftCard.new(
    :RegaloPerrosLegendarios2011,
    _INTL("Perros Legendarios de INV2011"),
    PBSpecies::SUICUNE,
    _INTL("Suicune, Entei y Raikou del evento INV2011. ¡Todos variocolor!")
  ) {
    # Suicune
    @pokemon = PokeBattle_Pokemon.new(:SUICUNE,30,$Trainer)
    @pokemon.giveRibbon(PBRibbons::CLASSIC)
    @pokemon.ot = "INV2011"
    @pokemon.trainerID = 2211
    @pokemon.setItem(:ROWAPBERRY)
    @pokemon.pbLearnMove(:AQUARING)
    @pokemon.pbLearnMove(:AIRSLASH)
    @pokemon.pbLearnMove(:EXTREMESPEED)
    @pokemon.pbLearnMove(:SHEERCOLD)
    @pokemon.setNature(:RELAXED)
    @pokemon.form = 1
    maxIV = 1
    @pokemon.iv[rand(6)] = 31
    [0,1,2,3,4,5].shuffle.each do |i|
      next if @pokemon.iv[i]==31
      maxIV += 1
      @pokemon.iv[i] = 31
      break if maxIV>=3
    end
    @pokemon.makeShiny
    @pokemon.obtainMode = 4
    @pokemon.calcStats
    @pokemon.ballused = 15
    showHidePokemon rescue nil
    Kernel.pbAddPokemon(@pokemon,nil,true,false)
    showHidePokemon(false) rescue nil
    
    # Entei
    @pokemon = PokeBattle_Pokemon.new(:ENTEI,30,$Trainer)
    @pokemon.giveRibbon(PBRibbons::CLASSIC)
    @pokemon.ot = "INV2011"
    @pokemon.trainerID = 2211
    @pokemon.setItem(:CUSTAPBERRY)
    @pokemon.pbLearnMove(:SACREDFLARE)
    @pokemon.pbLearnMove(:CRUSHCLAW)
    @pokemon.pbLearnMove(:EXTREMESPEED)
    @pokemon.pbLearnMove(:HOWL)
    @pokemon.setNature(:ADAMANT)
    @pokemon.form = 1
    maxIV = 1
    @pokemon.iv[rand(6)] = 31
    [0,1,2,3,4,5].shuffle.each do |i|
      next if @pokemon.iv[i]==31
      maxIV += 1
      @pokemon.iv[i] = 31
      break if maxIV>=3
    end
    @pokemon.makeShiny
    @pokemon.obtainMode = 4
    @pokemon.calcStats
    showHidePokemon rescue nil
    Kernel.pbAddPokemon(@pokemon,nil,true,false)
    showHidePokemon(false) rescue nil
    
    # Raikou
    @pokemon = PokeBattle_Pokemon.new(:RAIKOU,30,$Trainer)
    @pokemon.giveRibbon(PBRibbons::CLASSIC)
    @pokemon.ot = "INV2011"
    @pokemon.trainerID = 2211
    @pokemon.setItem(:MICLEBERRY)
    @pokemon.pbLearnMove(:ZAPCANNON)
    @pokemon.pbLearnMove(:WEATHERBALL)
    @pokemon.pbLearnMove(:EXTREMESPEED)
    @pokemon.pbLearnMove(:AURASPHERE)
    @pokemon.setNature(:RASH)
    @pokemon.form = 1
    maxIV = 1
    @pokemon.iv[rand(6)] = 31
    [0,1,2,3,4,5].shuffle.each do |i|
      next if @pokemon.iv[i]==31
      maxIV += 1
      @pokemon.iv[i] = 31
      break if maxIV>=3
    end
    @pokemon.makeShiny
    @pokemon.obtainMode = 4
    @pokemon.calcStats
    showHidePokemon rescue nil
    Kernel.pbAddPokemon(@pokemon,nil,true,false)
    showHidePokemon(false) rescue nil
  })

elsif password == "P1K0R3J4" && GAME_CODE != "PERSEIDA"
  gifts.push(MysteryGiftCard.new(
    :RegaloPichu2010,
    _INTL("Pichu de PRI2010"),
    PBSpecies::PICHU,
    _INTL("Un Pichu especial del evento PRI2010. ¡Conoce Placaje Electrico!")
  ) {
    @pokemon = PokeBattle_Pokemon.new(:PICHU,30,$Trainer)
    @pokemon.giveRibbon(PBRibbons::CLASSIC)
    @pokemon.ot = "PRI2010"
    @pokemon.trainerID = 3050
    @pokemon.setItem(:EVERSTONE)
    @pokemon.pbLearnMove(:VOLTTACKLE)
    @pokemon.pbLearnMove(:ENDEAVOR)
    @pokemon.pbLearnMove(:ENDURE)
    @pokemon.makeShiny
    @pokemon.setNature(:JOLLY)
    maxIV = 1
    @pokemon.iv[rand(6)] = 31
    [0,1,2,3,4,5].shuffle.each do |i|
      next if @pokemon.iv[i]==31
      maxIV += 1
      @pokemon.iv[i] = 31
      break if maxIV>=3
    end
    @pokemon.happiness = 100
    @pokemon.obtainMode = 4
    @pokemon.ballused = 15
    @pokemon.calcStats
    showHidePokemon rescue nil
    Kernel.pbAddPokemon(@pokemon,nil,true,false)
    showHidePokemon(false) rescue nil
  })

elsif password == "T1M3"
  gifts.push(MysteryGiftCard.new(
    :RegaloCelebi2011,
    _INTL("Celebi de INV2011"),
    PBSpecies::CELEBI,
    _INTL("Un Celebi shiny. ¡Conoce Deseo Cura!")
  ) {
    @pokemon = PokeBattle_Pokemon.new(:CELEBI,50,$Trainer)
    @pokemon.giveRibbon(PBRibbons::CLASSIC)
    @pokemon.ot = "INV2011"
    @pokemon.trainerID = 1211
    @pokemon.setItem(:JABOCABERRY)
    @pokemon.pbLearnMove(:HEALINGWISH)
    @pokemon.setNature(:MODEST)
    maxIV = 1
    @pokemon.iv[rand(6)] = 31
    [0,1,2,3,4,5].shuffle.each do |i|
      next if @pokemon.iv[i]==31
      maxIV += 1
      @pokemon.iv[i] = 31
      break if maxIV>=3
    end
    @pokemon.makeShiny
    @pokemon.happiness = 100
    @pokemon.obtainMode = 4
    @pokemon.ballused = 15
    @pokemon.calcStats
    showHidePokemon rescue nil
    Kernel.pbAddPokemon(@pokemon,nil,true,false)
    showHidePokemon(false) rescue nil
  })

elsif password == "909TEAMUP06"
  gifts.push(MysteryGiftCard.new(
    :RegaloFuecocoRoy,
    _INTL("Fuecoco de Roy"),
    PBSpecies::FUECOCO,
    _INTL("¡El Fuecoco de Roy de la serie de Horizontes Pokémon!")
  ) {
    @pokemon = PokeBattle_Pokemon.new(:FUECOCO,5,$Trainer)
    @pokemon.giveRibbon(PBRibbons::CLASSIC)
    @pokemon.ot = "Roy"
    @pokemon.trainerID = 230512
    maxIV = 1
    @pokemon.iv[rand(6)] = 31
    [0,1,2,3,4,5].shuffle.each do |i|
      next if @pokemon.iv[i]==31
      maxIV += 1
      @pokemon.iv[i] = 31
      break if maxIV>=3
    end
    @pokemon.obtainMode = 4
    @pokemon.ballused = 15
    @pokemon.calcStats
    showHidePokemon rescue nil
    Kernel.pbAddPokemon(@pokemon,nil,true,false)
    showHidePokemon(false) rescue nil
  })

elsif password == "D0T1STPARTNER"
  gifts.push(MysteryGiftCard.new(
    :RegaloQuaxlyDot,
    _INTL("Quaxly de Dot"),
    PBSpecies::QUAXLY,
    _INTL("¡El Quaxly de Dot de la serie de Horizontes Pokémon!")
  ) {
    @pokemon = PokeBattle_Pokemon.new(:QUAXLY,5,$Trainer)
    @pokemon.giveRibbon(PBRibbons::CLASSIC)
    @pokemon.ot = "Dot"
    @pokemon.trainerID = 230414
    maxIV = 1
    @pokemon.iv[rand(6)] = 31
    [0,1,2,3,4,5].shuffle.each do |i|
      next if @pokemon.iv[i]==31
      maxIV += 1
      @pokemon.iv[i] = 31
      break if maxIV>=3
    end
    @pokemon.obtainMode = 4
    @pokemon.ballused = 15
    @pokemon.calcStats
    showHidePokemon rescue nil
    Kernel.pbAddPokemon(@pokemon,nil,true,false)
    showHidePokemon(false) rescue nil
  })

elsif password == "L1K0W1TH906"
  gifts.push(MysteryGiftCard.new(
    :RegaloSprigatitoLiko,
    _INTL("Sprigatito de Liko"),
    PBSpecies::SPRIGATITO,
    _INTL("¡El Sprigatito de Liko de la serie de Horizontes Pokémon!")
  ) {
    @pokemon = PokeBattle_Pokemon.new(:SPRIGATITO,5,$Trainer)
    @pokemon.giveRibbon(PBRibbons::CLASSIC)
    @pokemon.ot = "Liko"
    @pokemon.trainerID = 230414
    maxIV = 1
    @pokemon.iv[rand(6)] = 31
    [0,1,2,3,4,5].shuffle.each do |i|
      next if @pokemon.iv[i]==31
      maxIV += 1
      @pokemon.iv[i] = 31
      break if maxIV>=3
    end
    @pokemon.obtainMode = 4
    @pokemon.ballused = 15
    @pokemon.calcStats
    showHidePokemon rescue nil
    Kernel.pbAddPokemon(@pokemon,nil,true,false)
    showHidePokemon(false) rescue nil
  })

elsif password == "Y0AS0B1B1R1B1R1"
  gifts.push(MysteryGiftCard.new(
    :RegaloPawmotYOASOBI,
    _INTL("Pawmot de YOASOBI"),
    PBSpecies::PAWMOT,
    _INTL("¡Pawmot especial de la colaboración con YOASOBI!")
  ) {
    @pokemon = PokeBattle_Pokemon.new(:PAWMOT,20,$Trainer)
    @pokemon.giveRibbon(PBRibbons::CLASSIC)
    @pokemon.ot = "YOASOBI"
    @pokemon.trainerID = 231118
    maxIV = 1
    @pokemon.iv[rand(6)] = 31
    [0,1,2,3,4,5].shuffle.each do |i|
      next if @pokemon.iv[i]==31
      maxIV += 1
      @pokemon.iv[i] = 31
      break if maxIV>=3
    end
    @pokemon.pbLearnMove(:DOUBLESHOCK)
    @pokemon.obtainMode = 4
    @pokemon.ballused = 15
    @pokemon.calcStats
    showHidePokemon rescue nil
    Kernel.pbAddPokemon(@pokemon,nil,true,false)
    showHidePokemon(false) rescue nil
  })
end