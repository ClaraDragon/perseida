####################################################
# BASE DE REGALO MISTERIOSO PARA EL SCRIPT DE REGALOS MISTERIOSOS AVANZADOS DE CLARA
# Tómalo de base para crear o añadir nuevos eventos. Puedes entregar más de un evento a la vez.
# Subelo a Pastebin o a Github como un txt y coge el enlace del archivo en RAW.
####################################################
  m = Time.new.month
  d = Time.new.day
  ret = false
  #Comprueba que el jugador ya tenga ese regalo especifico, para objetos es mejor usar otro tipo de check.
  #Para pokemon con evolucion, debes comprobar que esten ambas especies.
  gifts.push(MysteryGiftCard.new(
    :RegaloGSBall,
    _INTL("GS Ball"),
    [PBItems::GSBALL, :item],  # Array con el icono y el tipo
    _INTL("Se dice que si llevas la enigmática GS Ball al Encinar, puede suceder algo...")
  ) {
    @item=PBItems::GSBALL
    showHideItem(true) rescue nil
    Kernel.pbItemBall(@item)
    showHideItem(false) rescue nil
  })
  if (m == 12 && (d >= 25)) || (m == 1 && (d <= 10))
    gifts.push(MysteryGiftCard.new(
      :RegaloJirachiNEWYEAR,
      _INTL("Jirachi de año nuevo"),
      PBSpecies::JIRACHI,
      _INTL("Un Jirachi especial de año nuevo. ¡Conoce Cometa Draco!")
    ) {
      @pokemon = PokeBattle_Pokemon.new(:JIRACHI,5,$Trainer)
      @pokemon.giveRibbon(PBRibbons::CLASSIC)
      @pokemon.ot = "NEWYEAR"
      @pokemon.trainerID = 06260
      @pokemon.setItem(:LIECHIBERRY)
      @pokemon.pbLearnMove(:DRACOMETEOR)
      maxIV = 1
      @pokemon.iv[rand(6)] = 31
      [0,1,2,3,4,5].shuffle.each do |i|
        next if @pokemon.iv[i]==31
        maxIV += 1
        @pokemon.iv[i] = 31
        break if maxIV>=3
      end
      @pokemon.ballused = 15
      @pokemon.obtainMode = 4
      @pokemon.calcStats
      showHidePokemon rescue nil
      Kernel.pbAddPokemon(@pokemon,5,true,false)
      showHidePokemon(false) rescue nil
    })
  end
  if (m == 2 && (d >= 27)) || (m == 3 && (d <= 3))
    gifts.push(MysteryGiftCard.new(
      :RegaloMewPKMNDAY,
      _INTL("Mew del Pokemon Day"),
      PBSpecies::MEW,
      _INTL("Un Mew especial del Pokemon Day.")
    ) {
      @pokemon = PokeBattle_Pokemon.new(:MEW,5,$Trainer)
      @pokemon.giveRibbon(PBRibbons::CLASSIC)
      @pokemon.ot = "PKMNDAY"
      @pokemon.trainerID = 10160
      maxIV = 1
      @pokemon.iv[rand(6)] = 31
      [0,1,2,3,4,5].shuffle.each do |i|
        next if @pokemon.iv[i]==31
        maxIV += 1
        @pokemon.iv[i] = 31
        break if maxIV>=3
      end
      @pokemon.ballused = 15
      @pokemon.obtainMode = 4
      @pokemon.calcStats
      showHidePokemon rescue nil
      Kernel.pbAddPokemon(@pokemon,5,true,false)
      showHidePokemon(false) rescue nil
    })
  end
  if GAME_CODE == "PERSEIDA"
    gifts.push(MysteryGiftCard.new(
      :RegaloEeveePers3ida,
      _INTL("Eevee Vigoroso"),
      PBSpecies::EEVEE,
      _INTL("Un Eevee con el Emblema Vigor. ¡Conoce Cola Férrea!")
    ) {
      @pokemon = PokeBattle_Pokemon.new(:EEVEE,5,$Trainer)
      @pokemon.giveRibbon(PBRibbons::VIGORMARK)
      @pokemon.ot = "P3RS3IDA"
      @pokemon.trainerID = 190511
      @pokemon.pbLearnMove(:IRONTAIL)
      @pokemon.makeShiny
      maxIV = 1
      @pokemon.iv[rand(6)] = 31
      [0,1,2,3,4,5].shuffle.each do |i|
        next if @pokemon.iv[i]==31
        maxIV += 1
        @pokemon.iv[i] = 31
        break if maxIV>=3
      end
      @pokemon.ballused = 15
      @pokemon.obtainMode = 4
      @pokemon.calcStats
      showHidePokemon rescue nil
      Kernel.pbAddPokemon(@pokemon,5,true,false)
      showHidePokemon(false) rescue nil
    })
  end